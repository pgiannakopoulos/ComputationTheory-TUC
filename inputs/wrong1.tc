(* Wrong 1 *)

let x <- 5 : int;
const k : bool;

const start <- (): int => {
	limit <- readInt();
	counter <- 0;
	number <- 2;
	while number <= limit loop
		if prime(number) then
			counter <- counter + 1;
			writeInt(number);
			writeString(" ");
		fi;
		number <- number + 1;
	pool;
	writeString("\n");
	writeInt(counter);
	writeString("\n");
	return 0;
}